namespace Domain.Constants;

public abstract class Roles
{
    public const string Administrator = nameof(Administrator);
    public const string CedacriOperator = nameof(CedacriOperator);
    public const string BankOperator = nameof(BankOperator);
}